<section class="">
    <section class="container text-center">
        <div class="row">
            <div class="col">
                <h1>The Tech Blog</h1>
            </div>
        </div>
            <div class="card">
                <a class="btn card-block stretched-link text-decoration-none" id="openPostBtn"></a>
                <h5 class="card-header row mx-0">
                    <p class="col">{{ post.title }}</p>
                    <p class="col">{{ post.date_created }}</p>
                </h5>
                <div class="card-body">
                    <p class="card-text">{{ post.content }}</p>
                </div>
            </div>
            {{#each comments}}
            <div class="card">
                <h5 class="card-header row mx-0">
                    <p class="col"></p>
                </h5>
                <div class="card-body">
                    <p class="card-text">{{ content }}</p>
                    <p class="card-text">{{ user_Id }}</p>
                    <p class="card-text">{{ date_created }}</p>
                </div>
            </div>
            {{/each}}
            <div class="mb-3">
                <label for="commentInput" class="form-label">Comment</label>
                <textarea class="form-control" id="commentInput" rows="3"></textarea>
                <button type="button" class="btn btn-primary" id="commentButton">Submit Comment </button>
            </div>
        </div>
    </section>
</section>

<script src="js/comments.js"></script>